<UserControl x:Class="AssignmentEvaluator.WPF.Views.StudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             Background="White"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Grid.Row="0" 
                    HorizontalAlignment="Left"
                Width="100" Height="100" BorderThickness="0" Background="White">
                <Image Source="/Images/PreviousButton.png"/>
            </Button>

            <StackPanel Orientation="Vertical" Grid.Row="0" Grid.Column="1" >
                <StackPanel Orientation="Horizontal" Margin="0 20 0 0"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="현재 학생 : " />
                    <TextBlock x:Name="CurrentStudentNumber" Text="0" />
                    <TextBlock Text=" / " />
                    <TextBlock  Text="{Binding AllStudentCount}" />
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 20 0 0"
                        HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="채점완료 : " />
                    <TextBlock x:Name="CurrentCompletedCount" Text="0" />
                    <TextBlock Text=" / " />
                    <TextBlock Text="{Binding AllStudentCount}" />
                </StackPanel>

            </StackPanel>

            <Button x:Name="MoveNext" Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right"
                Width="100" Height="100" BorderThickness="0" Background="White">
                <Image Source="/Images/NextButton.png"/>
            </Button>
        </Grid>

        <Border BorderThickness="1"  BorderBrush="Black"/>

        <StackPanel Orientation="Horizontal" Margin="0 10 0 10">
            <TextBlock Text="이름 : "  Margin="30 0 0 0"/>
            <TextBlock Text="{Binding CurrentStudent.Name}" MinWidth="150" />

            <TextBlock Text="학번 : "  Margin="30 0 0 0"/>
            <TextBlock Text="{Binding CurrentStudent.StudentId}" MinWidth="150" />

            <TextBlock Text="채점상태 : "  Margin="30 0 0 0"/>
            <TextBlock Text="{Binding CurrentStudent.IsEvaluationCompleted}" MinWidth="150" />

            <TextBlock Text="지각여부 : " Margin="30 0 0 0"/>
            <CheckBox IsChecked="False" />
        </StackPanel>

        <Border BorderThickness="1"  BorderBrush="Black"/>

        <StackPanel Orientation="Horizontal">
            <ItemsControl ItemsSource="{Binding CurrentStudentProblems}" Margin="20 0 0 0"
                 Width="750"
                 MinHeight="150" 
                 HorizontalContentAlignment="Center" HorizontalAlignment="Center">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="문제번호"/>
                                <TextBlock Text="{Binding ProblemNumber}" MinWidth="50" HorizontalAlignment="Right"/>
                            </StackPanel>

                            <StackPanel 
                                Margin="50 0 50 0"
                                Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="점수" HorizontalAlignment="Center"/>
                                <TextBox Text="{Binding Score}" Width="50" 
                                     HorizontalAlignment="Center" HorizontalContentAlignment="Center"/>
                            </StackPanel>

                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock HorizontalAlignment="Center" Text="코멘트"/>
                                <TextBox Text="{Binding Comment}" Width="450" Height="80"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>


            <StackPanel Margin="15 150 0 0">
                <Button x:Name="CompleteEvaluation" Width="100" Height="40" Content="완료" />
                <Button x:Name="SaveAllToJson" Width="100" Height="40" Content="JSON저장" Margin="0 15 0 15"/>
                <Button x:Name="SaveAllToCSV" Width="100" Height="40" Content="CSV저장"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</UserControl>

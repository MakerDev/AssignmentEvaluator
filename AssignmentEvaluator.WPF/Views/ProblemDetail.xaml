<UserControl x:Class="AssignmentEvaluator.WPF.Views.ProblemDetail"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"      
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:AssignmentEvaluator.WPF.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <Style x:Key="TestCaseStyle" >

        </Style>
    </UserControl.Resources>

    <StackPanel>
        <TextBlock 
            Text="{Binding Id, StringFormat=문제{}, FallbackValue=문제3}" 
            FontSize="28"/>

        <TextBox Text="{Binding Feedback, FallbackValue=피드백, Mode=TwoWay}" 
                 Width="550" Height="50" 
                 TextWrapping="Wrap"/>
        
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding TestCases}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </StackPanel>
</UserControl>
